info:
  name: Publish a reviewed request
  type: http
  seq: 9
http:
  method: PATCH
  url: '{{baseUrl}}/admin/requests/:id/publish'
  params:
  - name: id
    value: '{{requestId}}'
    type: path
  auth:
    type: bearer
    token: '{{admin.token}}'
  headers:
  - name: Accept
    value: application/json
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
  - expression: res.status
    operator: in
    value: 200,201
