info:
  name: Get own profile details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/profile"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{user.token}}"

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: 200,201

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
