info:
  name: View own donation history
  type: http
  seq: 12
http:
  method: GET
  url: '{{baseUrl}}/donors/donations'
  auth:
    type: bearer
    token: '{{donor.token}}'
  headers:
  - name: Accept
    value: application/json
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
  - expression: res.status
    operator: in
    value: 200,201
