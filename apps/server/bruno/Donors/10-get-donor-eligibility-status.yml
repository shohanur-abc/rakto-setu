info:
  name: Get donor eligibility status
  type: http
  seq: 10
http:
  method: GET
  url: '{{baseUrl}}/donors/eligibility'
  auth:
    type: bearer
    token: '{{token}}'
  headers:
  - name: Accept
    value: application/json
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
  - expression: res.status
    operator: in
    value: 200,201
